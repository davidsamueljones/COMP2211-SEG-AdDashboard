package group33.seg.controller.events;

public class StatisticChangeEvents {
  // model being used
  // table to manipulate

  public void propertyChange(/* Query object? */ ) {
    // TODO:
    // 1. Update property in table (if shown)
    // !Property change could hide/show column
  }

  public void queryChange(/* Query object? */ ) {
    // TODO:
    // 1. Requery line data
    // 2. Update line data in table (if shown)
  }
}
